if (window.innerWidth < 768) {
    // Lakukan sesuatu untuk tampilan kecil
  } else if (window.innerWidth >= 768 && window.innerWidth < 1024) {
    // Lakukan sesuatu untuk tampilan sedang
  } else {
    // Lakukan sesuatu untuk tampilan besar
  }